# --- Python venv & bytecode ---
.venv/
__pycache__/
*.py[cod]
*.pyo
*.pyd

# --- Build / packaging ---
build/
dist/
*.egg-info/
.eggs/

# --- Test / tooling caches ---
.pytest_cache/
.mypy_cache/

# --- Local machine-only artifacts ---
env/env.txt

# --- Logs and caches we do NOT track ---
experiments/logs/

# --- Misc ---
*.bak
.git_bad/
fsck.txt
_audited/

# Optional vector exports
figures/*.svg

.git_corrupt_backup/
.ruff_cache/

/log-project-src.zip
.coverage
### LaTeX build artifacts ###
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
*.lof
*.lot
*.nav
*.snm
*.vrb
*.xdv
*.bcf
*.run.xml
# disabled: paper-repo/

log-project-paper-package/
# LaTeX build in paper-repo (keep repo clean)
# disabled: paper-repo/*.aux
# disabled: paper-repo/*.bbl
# disabled: paper-repo/*.blg
# disabled: paper-repo/*.log
# disabled: paper-repo/*.out
# disabled: paper-repo/*.toc
# disabled: paper-repo/*.lof
# disabled: paper-repo/*.lot
# disabled: paper-repo/*.synctex.gz
# disabled: paper-repo/*.fls
# disabled: paper-repo/*.fdb_latexmk
# disabled: paper-repo/*.bcf
# disabled: paper-repo/*.run.xml
# disabled: paper-repo/*.xdv
# disabled: paper-repo/*.nav
# disabled: paper-repo/*.snm
# disabled: paper-repo/*.vrb
# LaTeX build in paper-repo (keep repo clean)
paper-repo/*.aux
paper-repo/*.bbl
paper-repo/*.blg
paper-repo/*.log
paper-repo/*.out
paper-repo/*.toc
paper-repo/*.lof
paper-repo/*.lot
paper-repo/*.synctex.gz
paper-repo/*.fls
paper-repo/*.fdb_latexmk
paper-repo/*.bcf
paper-repo/*.run.xml
paper-repo/*.xdv
paper-repo/*.nav
paper-repo/*.snm
paper-repo/*.vrb
