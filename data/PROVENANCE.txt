notes: 2025-09-03 – audit: verified HASHES format and contents; 6 tests passing; protected JSON EOF policy enforced; removed BOM from pre-commit config and widened BOM guard to YAML; local .git/hooks/pre-commit had CRLF (non-tracked).
